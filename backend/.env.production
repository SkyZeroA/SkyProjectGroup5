## Example environment variables for backend
# Copy this to backend/.env and edit values for your environment.

# Environment: 'development' or 'production'
ENV=production

# Base URL used by the backend (frontend may also use this)
# For development keep as http://localhost:9099
BASE_URL=http://localhost:9099

# MySQL settings
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=SkyZeroDB

# Optional: a single DATABASE_URL if you prefer (not parsed automatically)
# DATABASE_URL=mysql+mysqlconnector://user:password@host:port/dbname
